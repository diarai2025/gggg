# DIAR AI Marketing CRM

Полнофункциональная CRM система для управления маркетингом с интеграцией AI и рекламных кампаний.

## 🚀 Технологии

### Frontend
- **React** + **TypeScript** - UI библиотека и типизация
- **Vite** - Сборщик и dev-сервер
- **Tailwind CSS** - Стилизация
- **Radix UI** - Компоненты UI
- **Supabase** - Аутентификация и база данных
- **Zod** - Валидация данных
- **React Hook Form** - Управление формами

### Backend
- **Node.js** + **Express** - Серверная часть
- **TypeScript** - Типизация
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** (Supabase) - База данных
- **Zod** - Валидация данных на сервере

## 📁 Структура проекта

```
diarai/
├── server/              # Backend сервер
│   ├── src/
│   │   ├── controllers/ # Контроллеры (бизнес-логика)
│   │   ├── routes/      # API маршруты
│   │   ├── middleware/  # Middleware (auth, validation, errors)
│   │   ├── validations/ # Схемы валидации Zod
│   │   ├── db/          # Prisma клиент
│   │   └── utils/       # Утилиты
│   ├── prisma/
│   │   ├── schema.prisma # Схема базы данных
│   │   └── seed.ts      # Тестовые данные
│   └── package.json
├── src/                 # Frontend приложение
│   ├── components/      # React компоненты
│   ├── contexts/        # React контексты
│   ├── hooks/           # Кастомные хуки
│   ├── lib/             # Утилиты и API клиент
│   └── styles/          # Стили
└── package.json
```

## 🛠️ Установка и запуск

### Предварительные требования

- Node.js 18+ и npm
- Аккаунт Supabase (для базы данных и аутентификации)

### 1. Клонирование и установка зависимостей

```bash
# Установка зависимостей фронтенда
npm install

# Установка зависимостей бэкенда
cd server
npm install
```

### 2. Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Получите `DATABASE_URL` из Settings → Database → Connection string → URI
3. Замените `[YOUR-PASSWORD]` на ваш пароль базы данных

### 3. Настройка Backend

```bash
cd server

# Создайте файл .env (на основе .env.example)
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"
# PORT=3001
# NODE_ENV=development

# Генерация Prisma клиента
npm run prisma:generate

# Применение миграций (создание таблиц)
npm run prisma:migrate

# Заполнение тестовыми данными (опционально)
npm run prisma:seed

# Запуск сервера
npm run dev
```

Сервер будет доступен на `http://localhost:3001`

### 4. Настройка Frontend

```bash
# В корневой директории проекта

# Создайте файл .env (если нужен)
# VITE_API_URL=http://localhost:3001

# Запуск dev-сервера
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

## 📚 API Endpoints

### Leads (Лиды)
- `GET /api/leads` - Получить все лиды
- `GET /api/leads/:id` - Получить лид по ID
- `POST /api/leads` - Создать лид
- `PUT /api/leads/:id` - Обновить лид
- `DELETE /api/leads/:id` - Удалить лид

### Deals (Сделки)
- `GET /api/deals` - Получить все сделки
- `GET /api/deals/:id` - Получить сделку по ID
- `POST /api/deals` - Создать сделку
- `PUT /api/deals/:id` - Обновить сделку
- `DELETE /api/deals/:id` - Удалить сделку

### Tasks (Задачи)
- `GET /api/tasks` - Получить все задачи
- `GET /api/tasks/:id` - Получить задачу по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу

### Campaigns (Кампании)
- `GET /api/campaigns` - Получить все кампании
- `GET /api/campaigns/:id` - Получить кампанию по ID
- `POST /api/campaigns` - Создать кампанию
- `PUT /api/campaigns/:id` - Обновить кампанию
- `DELETE /api/campaigns/:id` - Удалить кампанию

### User (Пользователь)
- `GET /api/user/profile` - Получить профиль пользователя
- `PUT /api/user/plan` - Обновить план подписки

### CRM & Dashboard
- `GET /api/crm/stats` - Статистика CRM
- `GET /api/crm/all` - Все данные CRM
- `GET /api/dashboard/stats` - Статистика дашборда

### Health Check
- `GET /health` - Проверка работоспособности сервера

## 🔐 Аутентификация

### Режим разработки

В режиме разработки используется `devAuthMiddleware`, который принимает заголовки:
- `x-user-id` - ID пользователя
- `x-user-email` - Email пользователя

**Пример запроса:**
```bash
curl -H "x-user-id: user-123" \
     -H "x-user-email: user@example.com" \
     http://localhost:3001/api/leads
```

### Продакшен

В продакшене замените `devAuthMiddleware` на `authMiddleware` в `server/src/index.ts` для проверки JWT токенов из Supabase.

## ✅ Валидация данных

Все API endpoints используют валидацию через Zod:
- Валидация на клиенте (frontend)
- Валидация на сервере (backend)
- Автоматическая обработка ошибок валидации

## 🗄️ База данных

Модели данных:
- **User** - Пользователи системы
- **Lead** - Лиды (потенциальные клиенты)
- **Deal** - Сделки
- **Task** - Задачи
- **Message** - Сообщения в чате
- **Campaign** - Рекламные кампании

Подробнее о структуре БД см. `server/prisma/schema.prisma`

## 📝 Скрипты

### Frontend
```bash
npm run dev      # Запуск dev-сервера
npm run build    # Сборка для продакшена
```

### Backend
```bash
npm run dev              # Запуск в режиме разработки
npm run build            # Сборка TypeScript
npm start                # Запуск продакшен версии
npm run prisma:generate  # Генерация Prisma клиента
npm run prisma:migrate   # Применение миграций
npm run prisma:studio    # Открыть Prisma Studio
npm run prisma:seed      # Заполнить тестовыми данными
```

## 🚧 Что нужно завершить

- [ ] Добавить тесты (unit и integration)
- [ ] Настроить CI/CD
- [ ] Добавить документацию API (Swagger/OpenAPI)
- [ ] Реализовать полную интеграцию с Supabase Auth в продакшене
- [ ] Добавить rate limiting
- [ ] Настроить логирование (Winston/Pino)
- [ ] Добавить мониторинг и метрики

## 📖 Дополнительная документация

- [Backend README](server/README.md) - Подробная документация по серверу
- [Supabase Setup](SUPABASE_SETUP.md) - Настройка Supabase
- [Validation Setup](VALIDATION_SETUP.md) - Настройка валидации

## 📄 Лицензия

MIT
